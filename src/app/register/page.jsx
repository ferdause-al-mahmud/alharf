import React from 'react';
import RegisterForm from '../Components/RegisterForm';
export const metadata = {
    title: "Register"
};
const page = () => {
    return (
        <div>
            <RegisterForm />
        </div>
    );
};

export default page;