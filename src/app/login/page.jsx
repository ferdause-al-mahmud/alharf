import React from 'react';
import LoginForm from '../Components/LoginForm';
export const metadata = {
    title: "Login"
};

const page = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default page;